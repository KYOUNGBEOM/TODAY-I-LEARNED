빅오 표기법(Big - O notation)
- 복잡도에 가장 영향을 많이 끼치는 항의 상수인자를 빼고 나머지 항을 없애서 복잡도를 나타내는 표기법

빅오 표기법 순서
n! > 2^n > n^2 > nlogn > n > logn > 1

상수 시간복잡도 O(1)
- 입력크기와 상관없이 일정한 시간복잡도를 가지는 것
- 입력과 출력 : cin, cout, scanf, printf
- 곱하기 : a[2] *= 2;
- 간단한 비교 if 문 : if(a[2] == 2)
- 배열 인덱스 참조 : int a[3] = {1, 2, 3};

시간복잡도를 알고싶으면 코드를 디버깅해보면 된다!
디버깅 이후 나오는 결과들을 바탕으로 손코딩하면서 그림으로 그려 시간복잡도를 파악하자! 

공간복잡도
- 입력 크기에 대하여 어떠한 알고리즘이 실행되는데 필요한 메모리 공간의 양
- 사실 공간복잡도의 개념은 문제를 푸는데 잘 사용되지 않기 때문에 보통 문제를 풀 때는 2가지 방법을 기반으로 범위를 잡는다.
  1. 최대범위
  2. 메모리제한 : 대부분의 문제는 1000만까지는 해보자
