빌트인
- 값 타입, 연산자, 오브젝트를 JS 코드를 처리하는 영역에 사전에 만들어 놓은 것
- 빌트인 값 타입 / 빌트인 연산자(Operator) / 빌트인 오브젝트

빌트인 오브젝트 형태
- 빌트인 Number 오브젝트 / 123과 같은 숫자, 상수, 지수를 처리하는 오브젝트
- 10개가는 빌트인 오브젝트가 존재

빌트인 오브젝트 유형
- Number / String / Boolean / Object / Array / Function / Math / Date / JSSON / RegEXP / 글로벌(Global) 
